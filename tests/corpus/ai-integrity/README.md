# AI Integrity Corpus

The permanent minimized benchmark is generated deterministically by `tests/aiIntegrity/benchmark.mjs`: 200 nonredundant attack cases, 50 source conflicts, and 50 clean controls. Confirmed future defects should be stored here as small explicit fixtures, not random bulk output.
